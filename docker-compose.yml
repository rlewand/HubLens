services:
  postgres:
    image: postgres:16-alpine
    container_name: hublens-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: hublens
      POSTGRES_PASSWORD: hublens
      POSTGRES_DB: hublens
    volumes:
      - hublens_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hublens -d hublens"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  hublens_pg_data:
